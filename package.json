{
  "name": "ton-store",
  "version": "1.0.0",
  "main": "./build/index.html",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist .parcel-cache",
    "start": "npm run clean && parcel src/index.pug src/terms.pug --open",
    "pretty": "npx prettier --write .",
    "build": "npm run clean && parcel build src/index.pug src/terms.pug --public-url ./"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "2.0.0-beta.2",
    "@parcel/transformer-sass": "2.0.0-beta.2",
    "autoprefixer": "^10.2.6",
    "parcel": "^2.0.0-beta.2",
    "postcss-modules": "^4.1.3",
    "prettier": "^2.3.1",
    "rimraf": "^3.0.2"
  }
}
