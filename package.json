{
  "name": "ton-store",
  "version": "1.0.0",
  "main": "./build/index.html",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist .parcel-cache",
    "start": "npm run clean && parcel src/index.pug --open",
    "pretty": "npx prettier --write .",
    "build": "npm run clean && parcel build src/index.pug --public-url ./"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "2.0.0-beta.2",
    "@parcel/transformer-sass": "2.0.0-beta.2",
    "parcel": "^2.0.0-beta.2",
    "prettier": "^2.3.1",
    "rimraf": "^3.0.2"
  }
}
